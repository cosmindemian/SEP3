syntax = "proto3";

message Credentials {
  string email = 1;
  string password = 2;
}

message JwtToken {
  string token = 1;
}

service AuthenticationProvider{
  rpc login(Credentials) returns (JwtToken);
  rpc register(Credentials) returns (JwtToken);
}